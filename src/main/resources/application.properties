spring.application.name=csis125-lab01

# Codespaces forwards this port automatically and opens a preview tab.
server.port=8080

# Show a readable message instead of a blank page when something goes wrong.
server.error.include-message=always
